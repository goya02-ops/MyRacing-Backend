# JWT Secrets - HACER ESTO SI O SI: Cambiar estos valores en tu .env local

JWT_SECRET=your_access_token_secret_here_minimum_32_characters
JWT_REFRESH_SECRET=your_refresh_token_secret_here_minimum_32_characters_different_from_above


# Instrucciones para compas
    #  Copiar .env.example a .env
    #  Reemplazar los valores de ejemplo por secretos reales (Lucho los pasa por medios seguros)
    #  NO subir .env a Git (agregar a .gitignore)

# Una vez hecho esto, desestimar este archivo y seguir con el proyecto

**Estructura de carpetas:**
```
my-racing-backend/
├── .env                  ← NO subir a Git (tiene tus secretos reales)
├── .env.example          ← SÍ subir a Git (plantilla sin secretos)
├── .gitignore
├── package.json
├── tsconfig.json
└── src/
    ├── auth/
    ├── shared/
    └── ...